import React, { useState } from 'react';
 
function EventHandling() {
  // define useState count - inital value is 0
 
  // define the handleDecrement and handleIncrement here

 
  return (
    <div>
      {/* create two buttons to handle the events - handleIncrement and handleDecrement */}
    </div>
  );
}
 
export default EventHandling;
