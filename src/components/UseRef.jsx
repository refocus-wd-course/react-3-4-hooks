import React, { useRef, useEffect } from 'react';

const UseRef = () => {
  // define useRef here and null as the inital value
 
  // create useEffect
 
  return (
    // create a form here with name and submit button to demonstrate useRef
    <form>
      
    </form>
  );
}

export default UseRef;
