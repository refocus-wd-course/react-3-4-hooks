import React, { useState } from 'react';

const UseState = () => {
  // define the useState count here initial value - 0

  return (
    // create a div with button and add onClick handler
    <div>

    </div>
  );
}

export default UseState;
